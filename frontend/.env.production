# Same-origin deployment: nginx serves the SPA and proxies /v1/* on agent.shi330.xyz,
# so the API base must stay EMPTY (relative URLs). Setting an absolute URL here gets
# baked into every local `npm run build` too and silently points local pages at prod.
VITE_API_URL=
